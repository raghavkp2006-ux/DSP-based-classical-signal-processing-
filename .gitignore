# Python environments and bytecode
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# Runtime outputs and local evaluation data
uploads/
outputs/
eval_log.csv
err.txt
out.txt
signalchain.log

# Local test / demo recordings
*.wav
*.mp3
*.mpeg
*.flac

# Android/Gradle generated files
.gradle/
**/build/
local.properties

# Note: model checkpoints under models/ are intentionally tracked in git
# (small enough at ~50KB, LFS is not needed). JSON sidecars are also tracked.
